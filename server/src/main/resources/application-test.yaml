spring:
  datasource:
    url: "jdbc:hsqldb:mem:pum"
    username: "sa"
    password:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
logging:
  level:
    root: info
    sql: trace