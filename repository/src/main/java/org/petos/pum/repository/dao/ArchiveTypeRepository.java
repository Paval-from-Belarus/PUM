package org.petos.pum.repository.dao;

import org.petos.pum.repository.model.ArchiveType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ArchiveTypeRepository extends JpaRepository<ArchiveType, Short> {
}